# Images

Placeholder visuals are currently CSS gradients + inline SVG so the layout reads correctly
before real photography arrives.

When you have real assets, add them here and swap the references in `index.html` / `styles.css`:

- `hero.jpg` — full-bleed hero (1920×1080+, optimized). Pair with the gradient scrim already in CSS.
- `residence-1.jpg`, `residence-2.jpg`, `residence-3.jpg` — featured unit cards (4:3).
- `neighbourhood.jpg` — lifestyle / location section (3:2).
- `og-image.jpg` — 1200×630 social share card (referenced by Open Graph meta).
- `logo.svg` / `favicon.svg` — brand marks.

Optimize before committing (WebP/AVIF preferred, width-appropriate, lazy-loaded below the fold).
